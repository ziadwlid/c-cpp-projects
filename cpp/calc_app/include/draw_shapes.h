#ifndef DRAW_SHAPES_H
#define DRAW_SHAPES_H

class draw_shapes {
    private:
        int n {0};
    public:
    static void rectangle(int width, int length);
    static void square(int length);
};

#endif